<h1>List of current Users</h1>
<% if @current_user %>
  <h1><%= @current_user.name %></h1>
<% end %>

<% if @users %>
  <ul>
  <% @users.each do |user| %>
    <li>
      <%= "#{ user.name }, #{ user.id }" %>
    </li>
  <% end %>
  </ul>
<% end %>
